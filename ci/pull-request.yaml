steps:
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "docker-pulumi-cli", "../pulumi"]
    id: build-pulumi-cli

  - name: "docker-pulumi-cli"
    entrypoint: "pulumi"
    args: ["preview"]
    secretEnv: ["$PULUMI_ACCESS_TOKEN"]
    waitFor: build-pulumi-cli
